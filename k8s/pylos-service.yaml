apiVersion: v1
kind: Service
metadata:
  name: pylos
  namespace: pylos
  labels:
    app: pylos
spec:
  selector:
    app: pylos
  ports:
    - name: http
      port: 3000
      targetPort: 3000
      protocol: TCP
  type: ClusterIP
