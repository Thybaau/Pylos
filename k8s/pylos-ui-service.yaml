apiVersion: v1
kind: Service
metadata:
  name: pylos-ui
  namespace: pylos
  labels:
    app: pylos-ui
spec:
  selector:
    app: pylos-ui
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
  type: ClusterIP
